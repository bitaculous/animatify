#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'bourbon', '~> 3.1.8'

  gem 'sprockets', '~> 2.12.0'
end

group :development do
  gem 'rake', '~> 10.2.2'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end